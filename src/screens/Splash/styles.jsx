import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  
  container: {
    flex: 1,
    backgroundColor: '#4ae26b',
    justifyContent: 'center',
    alignItems: 'center',
  },

  title: {
    fontSize: 28,
    color: '#ffffff',
    fontFamily: 'Montserrat-Bold',
  },

});

export default styles;